{
	"name": "webhook-inpector",
	"type": "module",
	"private": true,
	"scripts": {
		"compose:up": "docker compose -f docker/docker-compose.yaml up -d",
		"compose:down": "docker compose -f docker/docker-compose.yaml -v down",
		"compose:exec": "docker compose -f docker/docker-compose.yaml exec -it app bash"
	}
}
